indices:
  mysite:
    include:
      - /**
    target: /query-index.json
    properties:
      title:
        select: head > meta[property="category"]
        value: |
          attribute(el, "content")

